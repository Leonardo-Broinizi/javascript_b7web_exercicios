function somar(n1, n2) {
    let resultado = n1 + n2;
    return resultado; // Usei o return que o professor ainda não ensinou pois já aprendi em outro lugar.
}

console.log(`Resultado: ${somar(6, 3)}`)

function nomeCompleto(nome, sobrenome) {
    console.log(`${nome} ${sobrenome}`);
}

nomeCompleto("Leonardo", "Broinizi");
nomeCompleto("João", "Silva");