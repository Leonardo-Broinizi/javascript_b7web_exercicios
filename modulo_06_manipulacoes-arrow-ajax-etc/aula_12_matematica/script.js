let novoValor = Math.PI; // Retorna o valor de PI

novoValor = Math.round(3.69); // Método de arredondamento matemático.
novoValor = Math.floor(3.99); // Sempre arredonda para baixo.
novoValor = Math.ceil(1.11); // Sempre arredonda para cima. 

novoValor = Math.abs( -9.3234); // Retorna o valor absoluto (positivo).

novoValor = Math.min(3, 35, 32, 1); // Retorna o menor número dos informados.
novoValor = Math.max(3, 35, 32, 1); // Retorna o maior número dos informados.

novoValor = Math.random(); // Retorna um número real aleatório entre 0 e 1.
novoValor = Math.floor(Math.random() * 101); // Essa combinação das funções Math.floor com Math.random serve para gerarmos números aleatórios até o número que quisermos (no exemplo, 100) e inteiros.

console.log(novoValor); 

